# Node.js dependencies
/node_modules

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
Thumbs.db

# .env file (contains sensitive information like passwords and API keys)
.env

# Frontend build folder (if using React)
frontend/build

# Database files (if stored locally)
database/*.sql
